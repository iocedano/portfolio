// const { getIconURL, checkIsFileExists, downloadIcon } =require('../download-icons');

import { JobPropTypes } from "../../src/components/Job";

// Note: Need to config jest to look at for the tools folde
// Need to mock child_process

describe('tools/download-icons', () => {
  describe('getIconURL', {
    // it('should return the image url');
  });
  describe('checkIsFileExists', () => {
    // it('should return true if the file exists');
    // it('should return false if the file does not exist');
  });
  describe('downloadIcon', () => {
    // it('should download the icon');
  });
});
